user:
userId:str,
name:str,
avatarUrl:str|null,
email:str,
username:str,
bio:str,
createdAt:datetime,
updatedAt:datetime

---

user custom claims set by clerk:
roles:{role:str, entityId:str, entityType:str}[]

// this is not a table, its the object structure of public metadata

---

workspace:
ownerId:string,
workspaceId:str,
workspaceSlug:str|null,
workspaceType:"personal"|"shared",
workspaceName:str,
plan: "free"|"pro"|"enterprise",
status: "active"|"locked",
createdAt:datetime,
updatedAt:datetime

---

subscription:
subId: str
workspaceId: str
plan: "pro" | "enterprise"
razorpaySubscriptionId: str | null
razorpayCustomerId: str | null
status: "active" | "past_due" | "canceled" | "incomplete"
startsAt: datetime
endsAt: datetime | null
lastPaymentAt: datetime | null
nextPaymentAt: datetime | null
createdAt: datetime

---

entity_members:
workspaceId: str,
entityId:str
userId: str,
entityType:"workspace" | "project" | "collection" | "paper",
role: "owner"|"admin"|"member"|"viewer",
grantedAt: datetime,
grantedBySystem:boolean,
grantedById:str|null

---

project:
workspaceId:str,
projectId:str,
name:str,
publicSlug: str,
description:str,
logoUrl:str|null,
bannerUrl:str|null,
visibility: "private"|"public"
createdAt: datetime,
updatedAt: datetime


---

collection:
workspaceId:str,
projectId:str,
collectionId:str,
name:str,
publicSlug: str,
description:str,
bannerUrl:str|null,
visibility:"private"|"public"
createdAt: datetime,
updatedAt: datetime,

---

paper:
workspaceId:str,
projectId:str|null,
collectionId:str|null,
paperId:str,
title:str,
publicSlug:str,
thumbnailUrl:str|null,
visibility:"private"|"public",
createdAt: datetime,
updatedAt: datetime,

---

paper_content:
paperId: str,
content: str

// this separate content table is for not burning the database when quering and returning an array of papers. but when a paper is fetched by id or slug, this will be fetched and returned in one go.


Store the schema like this in fastapi so it can be used both as pydantic types and orm models.

class Paper(SQLModel, table=True):  # 👈 This is BOTH a table and a schema
    __tablename__ = "papers"